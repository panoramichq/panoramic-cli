from typing import List, Tuple

from panoramic.cli.state import Action


def execute(actions: List[Action]) -> Tuple[List[Action], List[Action]]:
    """Execute actions and return successful and failed actions."""
    pass
